{
    "name": "ROS2 Dev Container",
    "build": {
       "context": "..",
       "dockerfile": "Dockerfile",
       "args": { 
           // any build args if needed, e.g. TARGETARCH if used
       }
    },
    "runArgs": [
       "${localWorkspaceFolder}/scripts/devcontainer_init_devices.sh"
    ],
    "workspaceFolder": "/workspaces/Cislune-RE-RASSOR",
    "remoteUser": "ros2dev"
}
